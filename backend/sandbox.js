/**
 * Runs untrusted code in a safe execution environment
 * and returns true if the code throws an error.
 * @param {string} code 
 */
export async function checkCodeThrowsError(code) {
    return false;
} 