/**
 * A simple assertion test that verifies whether `value` is truthy.
 * If it is not, an error is thrown with `message`, if available.
 */
declare function assert(value: boolean, message?: string): void;